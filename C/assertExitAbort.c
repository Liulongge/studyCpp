#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

int main() {
   int x = 10;
   printf("The value of x : %d\n", x);
   //exit(-1);
   //abort();
   //assert(x!=15);
   return -1;
   printf("Calling of exit()");
   return 0;
}